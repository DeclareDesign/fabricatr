% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_simple.R
\name{draw_count}
\alias{draw_count}
\title{Draw Poisson count data}
\usage{
draw_count(
  mean = apply_link(latent, link),
  N = length(mean),
  link = "identity",
  latent = NULL,
  quantile_y = NULL
)
}
\arguments{
\item{mean}{Mean count (lambda). Scalar or per-unit vector.}

\item{N}{Number of draws. Defaults to \code{length(mean)}.}

\item{link}{Link function (\code{"identity"}, \code{"log"}).}

\item{latent}{Latent variable (alternative to \code{mean} with log link).}

\item{quantile_y}{Optional quantile vector for \code{correlate}.}
}
\value{
Non-negative integer vector.
}
\description{
Draw Poisson count data
}
\examples{
fabricate(N = 5, rate = c(0, 1, 5, 10, 50),
          Y = draw_count(mean = rate))

}

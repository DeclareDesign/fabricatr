% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/levels.R
\name{cross_levels}
\alias{cross_levels}
\title{Create a full Cartesian product of declared levels}
\usage{
cross_levels(.by, ...)
}
\arguments{
\item{.by}{Character vector of level names to cross (must have been created
by \code{add_level} or \code{declare_level} in the same \code{fabricate}
call).}

\item{...}{Additional column expressions evaluated after crossing.}
}
\value{
A \code{fabricatr_level} object (used inside \code{fabricate}).
}
\description{
Produces all combinations of the specified levels (equivalent to SQL
CROSS JOIN). Use \code{link_levels} to sample N rows from the product with
an optional correlation structure.
}
\examples{
fabricate(
  countries = declare_level(N = 10, gdp = runif(N, 1, 10)),
  years     = declare_level(N = 5, shock = runif(N, 0, 1)),
  obs       = cross_levels(.by = c("countries", "years"), Y = gdp + shock)
)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_icc.R
\name{draw_binary_icc}
\alias{draw_binary_icc}
\title{Draw binary data with a target intra-cluster correlation}
\usage{
draw_binary_icc(clusters, prob = 0.5, ICC = 0, N = NULL)
}
\arguments{
\item{clusters}{Vector of cluster IDs.}

\item{prob}{Cluster success probability. Scalar or per-cluster vector.
Default 0.5.}

\item{ICC}{Target ICC. Default 0 (independent draws).}

\item{N}{Optional; must equal \code{length(clusters)}.}
}
\value{
Integer vector of 0s and 1s.
}
\description{
Uses the mixture-of-Bernoullis model: with probability \eqn{\sqrt{ICC}} each
unit takes the cluster-level draw; otherwise it takes an independent draw.
This produces an expected ICC of the specified value.
}
\examples{
clusters <- rep(1:10, each = 20)
y <- draw_binary_icc(clusters = clusters, prob = 0.5, ICC = 0.3)
summary(lm(y ~ factor(clusters)))$r.squared  # approx 0.3

}

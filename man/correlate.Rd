% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_multivariate.R
\name{correlate}
\alias{correlate}
\title{Generate a variable correlated with an existing variable}
\usage{
correlate(draw_handler, ..., given, rho)
}
\arguments{
\item{draw_handler}{Unquoted function name: a \code{draw_*} function or a
base R \code{r*} function.}

\item{...}{Arguments forwarded to \code{draw_handler} (e.g. \code{prob},
\code{mean}).}

\item{given}{Reference vector; the new variable will be rank-correlated with
this.}

\item{rho}{Target Spearman rank correlation in \eqn{[-1, 1]}.}
}
\value{
Numeric vector of length \code{length(given)}.
}
\description{
Uses a Gaussian copula to produce a draw from \code{draw_handler} whose
rank correlation with \code{given} is approximately \code{rho}. Works with
any \code{draw_*} function that accepts a \code{quantile_y} argument, and
with base R random-number generators (e.g. \code{rnorm}, \code{rpois}).
}
\examples{
score  <- rnorm(100, mean = 75, sd = 10)
offers <- correlate(draw_count, mean = 3, given = score, rho = 0.6)
cor(score, offers, method = "spearman")

# Works with base R generators too
y2 <- correlate(rnorm, mean = 0, sd = 1, given = score, rho = -0.5)

}

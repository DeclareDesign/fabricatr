% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/levels.R
\name{nest_level}
\alias{nest_level}
\title{Nest a level within the current hierarchy}
\usage{
nest_level(N, ...)
}
\arguments{
\item{N}{Rows per parent. Scalar or per-parent vector.}

\item{...}{Column expressions. \code{N} is the inner count (children per
parent, not total rows).}
}
\value{
A \code{fabricatr_level} object (used inside \code{fabricate}).
}
\description{
For each row in the current data frame, creates N child rows. Parent columns
are replicated across children. \code{N} may be a scalar (same count for
every parent) or a vector of length \code{nrow} of the parent (variable
children per parent).
}
\examples{
fabricate(
  villages = add_level(N = 20, v_income = rnorm(N)),
  citizens = nest_level(N = 5, income = v_income + rnorm(N))
)

}
